<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Gates the built-in docs tree (DocsProvider) so only inb4doc can read
         the default content dir; user-picked SAF projects are unaffected. -->
    <permission
        android:name="inb4doc.editor.docs.ACCESS"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="inb4doc"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Inb4doc">

        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".DocsProvider"
            android:authorities="inb4doc.editor.docs"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="inb4doc.editor.docs.ACCESS" />
    </application>
</manifest>
